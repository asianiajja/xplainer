<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label "Session name" %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="password">
    <%= f.label "Password (optional)" %><br>
    <%= f.password_field :password %>
  </div>
  
  <div class="field">
    <%= f.label :public %>
    <%= f.check_box :public %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
